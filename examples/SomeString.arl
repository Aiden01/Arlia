import dio;

func substr(str : string, index : integer, length : integer) : string {
    return (str[index] to str[length]);
}

func main() {
    @dio:display.writeln(substr("Hello, world!", 0, 6));
}

`output:
    world!
`
