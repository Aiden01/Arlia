import dio;

rule Alphabetic : array<char> = ('a' to 'z');

func main() {
    var chr1 : char = 'd';
    var chr2 : char = 'H';
    if (chr1 is Alphabetic && chr2.ToLower is Alphabetic)
        @dio:display.write("'" + chr1 + "' and '" + che2 + "' are both alphabetic!");
}
