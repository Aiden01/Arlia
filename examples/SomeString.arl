import dio;

func RemoveSubstr(str : string, index : integer, length : integer) : string {
    return (str[index] to str[length]);
}

func main() {
    @dio:display.writeln(RemoveSubstr("Hello, world!", 0, 6));
}

`output:
    world!
`
