import lib.dio;

rule even = (
    n = MIN to MAX        ` MIN and MAX are const `
    | n % == 0
);

func main() {
    if (6 is even) {
        @dio:display.writeln("Yes! 6 is an even number!");
    }
}
