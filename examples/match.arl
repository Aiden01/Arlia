import dio;

func main() {
	var guess : integer = -1;
	var secret : integer = random();
	while (guess != secret) {
		guess = @dio:input.ask("Guess number: ");
		match n --> guess {
			case (n > secret) @dio:display.writeln("Too hight!");
			case (n < secret) @dio:display.writeln("Too low!");
			case (n == secret) @dio:display.writeln("Yhea!");
		}
	}
}
