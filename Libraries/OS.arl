`
	The OS hearder file, manage system.
`

namespace OS {

	define use_system extern "use 'system', from 'msvcrt', in 'INCLUDE'";
	
	func system( cmd : string ) {
		use_system;
		extern "invoke system, " + cmd;
	}
}
